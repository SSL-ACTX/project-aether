[package]
name = "aether"
version = "0.1.0"
edition = "2021"

[dependencies]
byteorder = { version = "1.5", default-features = false }
libc = "0.2"
nix = "0.27"

[lib]
name = "aether"
path = "src/lib.rs"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = 'abort'
strip = true
